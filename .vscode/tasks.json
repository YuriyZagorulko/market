{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "start_django_compose",
            "presentation": {
                "reveal": "always"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "/bin/bash",
            "args": [
                "-c",
                "fuser -k 8000/tcp || true && docker stop $(docker ps -aq) || true && docker rm $(docker ps -aq) || true && docker network prune -f || true && docker-compose -f docker-compose.debug.yml up --build -d && sleep 5"
            ]
        }
    ]
}
